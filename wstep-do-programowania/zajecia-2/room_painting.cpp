#include <iostream>

int main() {
	// input
	float room_length, room_width, room_height, window_width, window_height, door_width, door_height;
	int windows_number, doors_number;
	const float paint_liter_area = 5;
	std::cout << "Please input room length, width and height. Then number of windows "
	
	
	// calculations
	total_room_area = 2 * room_length * room_height + 2 * room_width * room_height + room_width * room_height;
	window_area = windows_number
}
